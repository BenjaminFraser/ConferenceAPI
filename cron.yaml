# configure a cron task to repopulate conference announcements.
# This url must be set appropriately in main.py with a function and handler url
cron:
- description: Repopulate the announcement every 1 hour
  url: /crons/set_announcement
  schedule: every 1 hours